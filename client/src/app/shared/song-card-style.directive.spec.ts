import { SongCardStyleDirective } from './song-card-style.directive';

describe('SongCardStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SongCardStyleDirective();
    expect(directive).toBeTruthy();
  });
});
